git config --global user.email ""
git config --global user.user.name ""

git init
git remote add origin "link do repositorio no github"

git status
git add .
git commit -m "mensagem referente a alteração"
git push origin master


https://gist.github.com/julianobailao

redimencionar a imagem em tamanho proporcional
adicionar as outras imagens e textos
como fazer identação pelo atom